# Bugs to fix

- When trying to loading a model that errors out (cube.rvox) in this case, like 500mb of memory gets allocated very quickly and doesn't get deallocated, which is concerning as to why that much and where.
